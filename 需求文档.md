# SynVerse（神韵）— AI 写作引擎 需求文档

> 状态：V1 起草中

---

## 一、项目定位

面向个人作者的**单用户 AI 辅助小说写作平台**。

不同于通用大模型直接写作，SynVerse 的核心差异化：

1. **学文笔** — 学习优秀小说的文学审美，写出来的东西不再"AI 腔"
2. **记设定** — 精确记住角色关系、伏笔、世界观，杜绝前后矛盾
3. **合流程** — 适配"大纲→迭代→续写"的创作习惯，不强行改变工作方式
4. **可进化** — 智能体和工作流可随作品规模扩展而平滑升级

---

## 二、功能模块

### 2.1 作品管理（主页 / 工作台）

用户进入系统后的初始页面，统一管理所有小说作品。

- **作品卡片展示**：封面、书名、类型标签、创作进度（卷/章/字数）、最后编辑时间
- **悬停交互**：卡片悬停时显示半透明遮罩层 + 作品简介
- **搜索**：按标题搜索
- **排序**：按创建时间、更新时间
- **筛选**：按类型标签筛选
- **新建作品**：点击"创建小说"按钮，弹出创建表单
- **空状态**：无作品时显示引导提示，引导用户创建第一部小说

### 2.2 基本信息

每本小说的元数据配置：

- 标题
- 封面（上传图片）
- 简介
- 类型标签：爽文、种田文、系统流、玄幻、都市、仙侠等（支持多选）
- 目标章节数：默认 500 章
- 目标每章字数：默认 2000 字（日更两章 4000 字有助于拿平台奖励）

### 2.3 核心架构（大纲生成）

这是整个写作流程的**蓝图**，融合核心创意、角色设定、世界观设定，由 AI 生成章级大纲。

**输入（用户填写）：**

| 输入项 | 说明 |
|--------|------|
| 核心种子 | 核心创意、核心矛盾、故事主线一句话概括 |
| 角色设定 | 主角、配角、反派的基本信息、性格、动机 |
| 世界观 | 世界规则、背景设定、修炼体系/魔法体系等 |

**AI 生成输出：**

- **小说大纲**：融合种子、角色、世界观，生成章级大纲（每章 1-2 句剧情概要）
- 人工审阅大纲，判断可行性
- 支持迭代完善：修改种子/角色/世界观后，重新生成或局部调整大纲

**空状态**：刚打开时提示填写核心种子、角色、世界观，点击"生成大纲"按钮触发 AI 生成。

### 2.4 章节管理

左侧边栏的章节目录管理：

- **章节列表**：编号、标题、更多操作按钮
- **新建章节**："开启新章"按钮
- **搜索章节**：按标题搜索
- **章节操作**：重命名、删除
- **统计信息**：底部显示总章节数
- **章节选择**：点击章节加载其内容到中央编辑区

### 2.5 AI 写作（核心创作）

写作区域有两种模式：

#### 小说模式
- 类似在线阅读器的界面，展示当前章节完整内容
- 可直接在界面中编辑章节内容
- 支持 Markdown 格式
- 默认加载最后一章；若没有章节，提示"开启新章"
- 选中章节 = 有继续修改创作的意图

#### AI 模式
- 对话式写作助手界面
- 左侧历史话题列表，右侧对话区域
- 支持深度思考（DeepSeek thinking）展示
- 底部对话框，输入写作指令
- **新章生成**：AI 根据大纲自动撰写新章节
- **续写/修改**：对已有内容提出修改意见，AI 重写

#### 智能增强
- 写作时自动检索**一致性知识图谱**，注入相关角色/设定信息
- 写作时自动检索**仿写知识库**，注入风格范例
- 生成结果内嵌展示，可在线编辑

### 2.6 节奏分析

AI 对已有章节进行多维度节奏分析：

**分析维度（8 维）：**
1. 信息密度
2. 情绪强度
3. 悬念设计
4. 冲突张力
5. 爽点密度
6. 节奏快慢
7. 对话占比
8. 描写占比

**输出形式：**
- 绘制**节奏曲线图**（折线图/雷达图）
- 标注高潮点和低谷点
- 输出设计缺陷和改进建议

**空状态**：提示"当前小说暂无章节内容，请先编写章节内容，再来进行节奏分析"。

### 2.7 一致性·知识图谱引擎

自动维护小说内部的设定一致性。

- **实体提取**：从章节文本中自动提取角色、地点、物品、事件等实体
- **关系图谱**：构建实体之间的动态关系网络（可视化展示）
- **实时检索**：AI 写作时实时检索相关角色/设定信息，注入 prompt
- **冲突检测**：检测新写内容是否与已有设定矛盾
- **图谱管理**：查看、编辑、补充知识图谱节点和关系

### 2.8 仿写·知识图谱引擎

基于本地优秀小说资料构建**风格知识库**。

- **资料入库**：上传小说 txt 文件 → 自动清洗 → 切片 → 向量化入库
- **场景分类**：按场景类型（日常/打斗/打脸/修炼/感情/阴谋等）分类标注
- **风格检索**：写作时根据当前场景类型自动检索相关范例段落
- **范例管理**：精选范例、标注质量、管理入库统计
- **效果**：让 AI 模仿优秀作品的剧情节奏和文笔风格，写出"真人感、高手范"

### 2.9 多 Agent 智能体

可扩展的智能体系统，用于辅助创作流程。

**系统内置智能体：**

| 智能体 | 职责 |
|--------|------|
| 写作智能体 | 负责章节生成、续写、修改 |
| 读者智能体 | 负责审读反馈（AI 味检测、节奏评估、爽点识别） |

**用户自定义智能体：**
- 用户可创建自定义智能体（如角色智能体、事件智能体、伏笔智能体等）
- 每个智能体可配置：角色定位、系统提示词、可用工具
- 创作过程中可调用不同智能体产生多样化反馈

### 2.10 技能系统（Skills）

可扩展的技能/插件机制（待细化设计）。

初步方向：
- 每个技能是一个独立的功能模块
- 可在工作流中被调用
- 支持用户自定义技能参数
- 示例技能：文风转换、人称切换、场景扩写、对话润色

### 2.11 工作流编排（Workflow）

灵活可调整的写作工作流引擎。

- **默认工作流**：大纲 → 选章节 → AI 生成 → 人工修改 → 审读反馈 → 修订
- **可视化编辑**：拖拽节点，自定义工作流步骤
- **节点类型**：AI 节点（调用智能体）、人工节点（等待用户输入）、条件节点（分支判断）、技能节点（调用技能）
- **状态持久化**：工作流执行状态保存，支持中断续跑

---

## 三、界面设计

### 3.1 主页（Dashboard）

```
┌─────────────────────────────────────────────────┐
│  我的小说                    共 3 部作品  [+ 创建小说]  [👤] │
├─────────────────────────────────────────────────┤
│  🔍 搜索小说标题...      [排序 ▼]  [筛选 ▼]         │
├──────────┬──────────┬──────────┬─────────────────┤
│ ┌──────┐ │ ┌──────┐ │ ┌──────┐ │                 │
│ │ 封面 │ │ │ 封面 │ │ │ 封面 │ │                 │
│ │      │ │ │      │ │ │      │ │                 │
│ └──────┘ │ └──────┘ │ └──────┘ │                 │
│ 穿成反派 │ 仙路长生 │ 诡秘世界 │                 │
│ 3卷·120章│ 1卷·45章 │ 0卷·0章  │                 │
│ ·58万字  │ ·22万字  │ ·0字     │                 │
│ 3小时前  │ 1天前    │ 5天前    │                 │
└──────────┴──────────┴──────────┴─────────────────┘
```

- 卡片网格布局，响应式适配
- 悬停卡片显示简介遮罩层
- 点击卡片进入工作界面

### 3.2 工作界面（三栏布局）

```
┌──────┬──────────────────────────────────────────────┐
│ 项目 │  AI模式 │ 小说模式 │ 基本信息 │ 核心架构 │ ...  │
│ 名称 │                                              │
│  ▼   ├──────────────────────────────────────────────┤
│      │                                              │
│ 章节 │         中央主编辑区（动态加载）              │
│ 目录 │         - AI 模式：对话界面                   │
│      │         - 小说模式：阅读/编辑器               │
│  ├ 1  │         - 基本信息：表单                     │
│  ├ 2  │         - 节奏分析：图表                     │
│  ├ 3  │         - 知识图谱：图谱可视化               │
│  │...│         - 工作流：流程图编辑器               │
│  │    │                                              │
│ 共计  │                                              │
│ 120章│                                              │
└──────┴──────────────────────────────────────────────┘
```

**顶部功能导航栏按钮：**
AI 模式、小说模式、基本信息、核心架构、节奏分析、一致性 RAG 引擎、仿写 RAG 引擎、多 Agent 智能体、Skills、工作流编排

**顶部导航与中央区域联动**：激活哪个导航按钮，中央区域就加载对应的功能模块。

---

## 四、技术架构

### 4.1 整体架构

```
┌──────────────────────────────────────────────────┐
│                   Frontend (Vue 3)                │
│  Element Plus │ Tiptap │ ECharts │ Vue Flow      │
└──────────────────────┬───────────────────────────┘
                       │ HTTP/SSE
┌──────────────────────┴───────────────────────────┐
│                Backend (FastAPI)                  │
│  LangGraph │ Celery │ Sentence-Transformers      │
└──┬────────┬────────┬────────┬───────────────────┘
   │        │        │        │
   ▼        ▼        ▼        ▼
┌──────┐ ┌─────┐ ┌──────┐ ┌──────┐
│PostgreSQL│ │Redis│ │MinIO │ │Ollama│
│+pgvector │ │     │ │      │ │      │
└──────┘ └─────┘ └──────┘ └──────┘
```

### 4.2 技术选型

| 层次 | 技术 | 用途 |
|------|------|------|
| 前端框架 | Vue 3 + Vite | SPA 应用 |
| UI 组件库 | Element Plus | 通用 UI 组件 |
| 富文本编辑 | Tiptap | 章节编辑（Markdown 支持） |
| 图表 | ECharts + vue-echarts | 节奏分析曲线 |
| 流程图 | Vue Flow | 工作流可视化编辑 |
| 工具库 | @vueuse/core, dayjs | 通用工具 |
| Markdown 渲染 | marked | AI 对话内容渲染 |
| 后端框架 | FastAPI | REST API + SSE |
| AI 编排 | LangGraph + LangChain | Agent 框架、工作流引擎、RAG 管线 |
| 异步任务 | Celery（Redis broker） | AI 生成、节奏分析等耗时任务 |
| 向量库 | pgvector（PostgreSQL 扩展） | 知识库向量检索 |
| 知识图谱 | NetworkX + PostgreSQL JSON | 实体关系图谱（V1 轻量方案） |
| 中文分词 | jieba | 实体提取、文本分析 |
| 嵌入模型 | BAAI/bge-small-zh-v1.5 | 中文文本向量化 |
| 对象存储 | MinIO | 封面图片、上传文件 |
| 主力模型 | DeepSeek API（deepseek-chat） | AI 写作、分析、大纲生成 |
| 辅助模型 | Ollama 本地模型 | 轻量任务（分词、分类等） |
| 部署 | Docker Compose | 运行在 uzentu（8GB/200GB） |

### 4.3 关键第三方库说明

#### 后端新增

| 库 | 版本 | 用途 |
|----|------|------|
| langgraph | >=1.0 | Agent 框架 + 工作流编排引擎 |
| langchain | >=0.3 | LLM 集成 + RAG 管线 |
| langchain-deepseek | >=0.1 | DeepSeek Chat 模型集成 |
| celery | >=5.4 | 异步任务队列（AI 生成、分析） |
| networkx | >=3.4 | 知识图谱构建与查询（内存图） |
| minio | >=7.2 | MinIO 对象存储客户端 |
| jieba | >=0.42 | 中文分词（实体提取） |
| python-multipart | >=0.0.18 | 文件上传支持 |
| aiofiles | >=24.0 | 异步文件操作 |

#### 前端新增

| 库 | 版本 | 用途 |
|----|------|------|
| @tiptap/vue-3 | ^2.x | 富文本编辑器（章节编写） |
| @tiptap/starter-kit | ^2.x | Tiptap 基础扩展包 |
| @tiptap/extension-placeholder | ^2.x | 编辑器占位符 |
| echarts | ^5.x | 图表渲染（节奏分析） |
| vue-echarts | ^7.x | ECharts Vue 3 封装 |
| @vue-flow/core | ^1.x | 工作流可视化编辑器 |
| @vue-flow/background | ^1.x | 工作流画布背景 |
| @vue-flow/controls | ^1.x | 工作流画布控制按钮 |
| @vueuse/core | ^12.x | Vue 组合式工具函数 |
| marked | ^15.x | Markdown 渲染 |
| dayjs | ^1.x | 日期格式化 |

---

## 五、版本规划

### V1 — 可用的写作工具（当前阶段）

**包含：**
- [ ] 作品管理（主页 / 工作台）
- [ ] 基本信息编辑
- [ ] 核心架构（种子 + 角色 + 世界观 → AI 生成大纲）
- [ ] 章节管理（左侧边栏）
- [ ] AI 写作（AI 模式对话 + 小说模式编辑）
- [ ] 知识库管理（上传 txt → 切片 → 向量入库）
- [ ] 仿写 RAG 引擎（写作时检索范例）
- [ ] 一致性 RAG 引擎（写作时检索设定）
- [ ] 节奏分析（8 维分析 + 图表）
- [ ] 多 Agent（写作智能体 + 读者智能体）

**不包含：**
- [ ] 自定义智能体
- [ ] Skills 系统
- [ ] 工作流编排（使用固定工作流）
- [ ] 多用户 / 权限
- [ ] 移动端适配
- [ ] 付费 / 订阅

### V2 — 可扩展的写作平台

- [ ] 工作流可视化编排
- [ ] Skills 插件系统
- [ ] 用户自定义智能体
- [ ] 知识图谱可视化编辑
- [ ] 导出功能（txt / epub）

### V3 — 智能化写作引擎

- [ ] 模型微调管线
- [ ] 自动伏笔管理
- [ ] 章节间影响分析
- [ ] 协同写作（多设备）

---

## 六、待讨论

- [ ] 是否需要版本管理（修改历史，可回退）
- [ ] 章节生成是同步（等结果）还是异步（后台任务+通知）
- [ ] 导出功能优先级（txt / epub）
- [ ] Skills 系统具体设计——是以 prompt 模板还是以代码插件形式
- [ ] 工作流编辑器——是否需要拖拽式可视化编辑，还是配置式
- [ ] 仿写知识库的"场景类型"分类体系如何定义
- [ ] 节奏分析的 8 个维度是否需要可配置
