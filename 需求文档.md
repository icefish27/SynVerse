# SynVerse（神韵）— AI 写作引擎 需求文档

> 状态：V1 起草中 | 最后更新：2026-05-16

---

## 一、项目定位

面向个人作者的**单用户 AI 辅助小说写作平台**。

### 核心差异化

1. **学文笔** — 学习优秀小说的文学审美，写出来的东西不再"AI 腔"
2. **记设定** — 精确记住角色关系、伏笔、世界观，杜绝前后矛盾
3. **合流程** — 适配"大纲→迭代→续写"的创作习惯，不强行改变工作方式
4. **可进化** — 智能体和工作流可随作品规模扩展而平滑升级

### 历史验证结论

基于此前的 novel 项目和 RAG 实验，已验证以下关键结论：

| 结论 | 来源 | 影响 |
|------|------|------|
| 范例文笔 > 规则约束 > 裸模型 | style_test.py 三组对比实验 | 仿写 RAG 引擎是核心差异化，必须做好 |
| RAG 检索 + 场景类型匹配可显著提升生成质量 | generate_v2.py 节拍式写作 | 写作管线必须基于场景类型进行差异化检索 |
| 智能体模式可行 | 写作智能体 + 读者智能体已产出 4 章 | 多 Agent 是 V1 可行方案 |
| DeepSeek 思考模式输出 reasoning_content | DeepSeek API 文档 | AI 模式界面必须展示思考过程 |
| 番茄平台 AI 检测严格 | 灵感收容/番茄读者喜好 | 写作风格规则是刚需，不是可选 |

---

## 二、功能模块

### 2.1 作品管理（主页 / 工作台）

用户进入系统后的初始页面，统一管理所有小说作品。

- **作品卡片**：封面、书名、类型标签、进度（卷/章/字数）、最后编辑时间
- **悬停交互**：半透明黑色遮罩 + 简介
- **搜索**：按标题搜索
- **排序/筛选**：按创建时间、更新时间、类型标签
- **空状态**：无作品时显示引导——"创建你的第一部小说"

### 2.2 基本信息

每本小说的元数据：

- 标题（7-10 字最佳，含热词 ≥2 个）
- 封面（上传图片 → MinIO）
- 简介（禁止写"慢热""前期铺垫"，直接亮卖点）
- 类型标签：爽文、种田文、系统流、玄幻、都市、仙侠、穿越等（多选）
- 目标章节数：默认 500 章
- 目标每章字数：默认 2000 字

### 2.3 核心架构（大纲生成）

写作流程的**蓝图**，用户填写核心要素，AI 生成章级大纲。

**输入（用户填写）：**

| 输入项 | 说明 |
|--------|------|
| 核心种子 | 核心创意、矛盾、故事主线一句话概括 |
| 角色设定 | 主角/配角/反派的基本信息、性格、动机 |
| 世界观 | 世界规则、背景、修炼/魔法体系等 |

**AI 生成输出：**
- 章级大纲（每章 1-2 句剧情概要），融合种子+角色+世界观
- 人工审阅，判断可行性
- 支持迭代：修改输入 → 局部调整或重新生成大纲

**空状态**：提示填写三项输入，点击"生成大纲"触发 AI。

### 2.4 章节管理

左侧边栏目录管理：

- 章节列表（编号、标题、更多按钮）
- 新建章节（"开启新章"按钮）
- 搜索章节
- 章节操作（重命名、删除）
- 底部统计：共 N 章
- 点击章节 → 加载内容到中央编辑区

### 2.5 AI 写作（核心）

#### 小说模式
- 在线阅读器风格界面，展示当前章节
- 支持 Markdown 编辑（Tiptap 编辑器）
- 默认加载最后一章；无章节提示"开启新章"

#### AI 模式
- 对话式写作助手，使用 **LiaoKit** 组件库（Vue 3 AI 聊天专用）
- 左侧历史话题列表，右侧对话区域
- **思考过程面板**（可折叠）：展示 DeepSeek `reasoning_content`（思维链）
- 底部对话框 + 发送按钮
- 支持 SSE 流式输出，打字机效果
- Markdown 实时渲染 + 代码高亮（markstream-vue 引擎）

**章节生成流程（基于历史 RAG 实验的管线）：**

```
用户：写第X章
       │
       ▼
① 读取小说大纲中该章剧情 → 读取上一章正文（上下文连续性）
       │
       ▼
② 场景类型识别（日常/打斗/打脸/修炼/感情/阴谋）
       │
       ▼
③ 并行检索：
   ├── 一致性 KG（Neo4j）：当前场景涉及的角色、地点、事件
   └── 仿写 RAG（pgvector）：该场景类型的优秀范例段落（Top 5）
       │
       ▼
④ 组装 Prompt：
   · 固定风格范例（精选，按场景类型绑定）
   · 风格规则（禁用词、句长节奏、情绪节奏等）
   · 小说总纲 + 角色设定
   · 上一章内容摘要
   · 一致性 KG 检索结果（相关设定）
   · 仿写 RAG 检索结果（参考段落）
       │
       ▼
⑤ DeepSeek API 生成（thinking mode enabled, stream=true）
       │
       ▼
⑥ 读者智能体审读 → 反馈 → 修订
       │
       ▼
⑦ 保存章节 → 更新 Neo4j 知识图谱（提取新实体和关系）
```

**关键参数：**
- 模型：`deepseek-v4-pro`（主力写作，context 1M tokens, max output 384K）
- 模型：`deepseek-v4-flash`（轻量任务，审读/分类/实体提取）
- thinking mode: `{"type": "enabled"}`，`reasoning_effort: "high"`
- temperature: 0.75（写作）/ 0.3（审读）
- max_tokens: 2000-4000/章（V1 单章生成）

### 2.6 节奏分析

AI 对已有章节进行多维度分析（用户标记为低优先级，简单实现即可）。

**分析维度（8 维）：**
信息密度、情绪强度、悬念设计、冲突张力、爽点密度、节奏快慢、对话占比、描写占比

**输出：**
- ECharts 雷达图 + 折线图
- 标注高潮/低谷
- 改进建议

**空状态**：提示"暂无章节内容，请先编写章节"。

### 2.7 一致性·知识图谱引擎

基于 **Neo4j 图数据库**，自动维护小说内部设定一致性。

- **实体提取**：用 jieba 分词 + DeepSeek flash 从章节中抽取角色、地点、物品、事件
- **关系构建**：存入 Neo4j（角色-属于-势力、角色-拥有-物品、事件-涉及-角色 等）
- **实时检索**：AI 写作时 Cypher 查询相关设定，注入 prompt
- **冲突检测**：新内容写入后对比已有图谱，标记潜在矛盾
- **图谱管理**：可视化查看（Neo4j Browser 集成或自建 D3.js 视图）、手动编辑节点和关系

### 2.8 仿写·知识图谱引擎

基于本地优秀小说构建**风格知识库**（RAG）。

- **资料入库**：上传 txt → 清洗（去噪声行、章节标题）→ 按段落切片（200-500 字/段）→ bge-small-zh 向量化 → pgvector 入库
- **场景分类**：日常/打斗/打脸/修炼/感情/阴谋 等（可扩展）
- **风格检索**：写作时按当前大纲场景描述 → 向量检索 Top 5 相似段落
- **固定范例**：每类场景精选 2-3 段固定范例（不检索，每次都复用）
- **范例管理**：精选标注、质量管理、入库统计

**与一致性 KG 的区别：**
| | 一致性 KG | 仿写 RAG |
|--|-----------|----------|
| 存储 | Neo4j（图） | pgvector（向量） |
| 内容 | 当前小说的设定 | 外部小说的片段 |
| 用途 | 记住设定、防矛盾 | 模仿文笔、找灵感 |
| 检索方式 | Cypher 查询 | 向量相似度 |

### 2.9 多 Agent 智能体

**V1 内置：**

| 智能体 | 职责 | 模型 |
|--------|------|------|
| 写作智能体 | 章节生成、续写、修改 | deepseek-v4-pro |
| 读者智能体 | 审读反馈（AI 味、节奏、爽点） | deepseek-v4-flash |

写作智能体的 Prompt 体系（基于 novel/写作智能体.md）：
- 角色定义 + 小说设定注入
- 写作规范（句长节奏、人物刻画、对话原则、情绪节奏）
- AI 味红线（禁用词列表、心理描写替代方案）
- 质量自检清单

读者智能体的反馈清单（基于 novel/读者agent.md）：
1. 继续看吗？2. 最爽的地方？3. 想跳的地方？4. 顺吗？5. 主角讨喜吗？6. AI 味检测？7. 一句话总结

**V2 扩展：**
- 用户可自定义智能体（角色定位 + 系统提示词 + 工具绑定）
- 示例：角色智能体（只负责某个配角的对白）、伏笔智能体（追踪伏笔埋设和回收）

### 2.10 技能系统（Skills）

Skills 和 Agent 的关系（回应用户疑问）：

```
Agent = 有角色身份 + 有目标的自主决策者（"我是写作智能体，我要写出好章节"）
Skill = 无身份的独立功能模块（"文风转换"是一个技能，谁都可以调用）
```

- Agent 可以调用 Skill 来完成子任务
- Skill 在工作流中作为节点被编排
- V1 不做，V2 开始设计

### 2.11 工作流编排（Workflow）

- **V1 固定工作流**：大纲 → 选章节 → AI 生成 → 审读反馈 → 修订
- **V2 可视化编排**：Vue Flow 拖拽节点，自定义工作流
- 节点类型：AI 节点（调用 Agent）、Skill 节点、人工节点（等用户确认）、条件分支
- 状态持久化，支持中断续跑

---

## 三、界面设计

### 3.1 主页

```
┌─────────────────────────────────────────────────┐
│  我的小说                    共 3 部作品  [+ 创建] [👤] │
├─────────────────────────────────────────────────┤
│  🔍 搜索...         [排序 ▼]  [筛选 ▼]             │
├──────────┬──────────┬──────────┬─────────────────┤
│ ┌──────┐ │ ┌──────┐ │ ┌──────┐ │                 │
│ │ 封面 │ │ │ 封面 │ │ │ 封面 │ │    空状态：       │
│ └──────┘ │ └──────┘ │ └──────┘ │   "你还没有作品，  │
│ 穿成反派 │ 仙路长生 │ 诡秘世界 │    创建第一部小说" │
│ 3卷·120章│ 1卷·45章 │ 0卷·0章  │                 │
│ 3小时前  │ 1天前    │ 5天前    │                 │
└──────────┴──────────┴──────────┴─────────────────┘
```

### 3.2 工作界面（三栏布局）

```
┌──────┬──────────────────────────────────────────────┐
│ 项目 │ AI模式│小说模式│基本信息│核心架构│节奏│一致性│仿写│Agent│...│
│  ▼   ├──────────────────────────────────────────────┤
│      │                                              │
│ 章节 │         中央主编辑区（动态加载）              │
│ 目录 │    AI模式：[历史话题] [对话+思考面板+对话框]  │
│  ├1   │    小说模式：[Tiptap Markdown 编辑器]        │
│  ├2   │    基本信息：表单                            │
│  ├3   │    核心架构：大纲+种子+角色+世界观编辑        │
│  ...  │    节奏分析：ECharts 图表                    │
│ 共N章 │    一致性RAG：知识图谱可视化                  │
│      │    仿写RAG：范例库管理                        │
│      │    Agent：智能体配置                           │
└──────┴──────────────────────────────────────────────┘
```

### 3.3 AI 聊天界面详细设计

基于 LiaoKit + 自定义思考面板：

```
┌─────────────────────────────────────────────────────┐
│ 历史话题列表              │  当前对话                 │
│ ┌─────────────────────┐   │ ┌─────────────────────┐ │
│ │ 第5章·系统激活      │   │ │ 🤔 思考过程（可折叠）│ │
│ │ 第4章·踢馆          │   │ │ "用户要写第5章系统   │ │
│ │ 第3章·药铺立威      │   │ │  激活，大纲显示这章  │ │
│ │ + 新话题            │   │ │  需要展示梦境系统... │ │
│ └─────────────────────┘   │ └─────────────────────┘ │
│                           │ ┌─────────────────────┐ │
│                           │ │ 📝 AI 回复（Markdown)│ │
│                           │ │ 正文内容...          │ │
│                           │ └─────────────────────┘ │
│                           ├─────────────────────────┤
│                           │ [输入框.........] [发送] │
└─────────────────────────────────────────────────────┘
```

---

## 四、技术架构

### 4.1 整体架构

```
┌──────────────────────────────────────────────────────────┐
│                    Frontend (Vue 3 + Vite)                │
│  Element Plus │ LiaoKit │ Tiptap │ ECharts │ Vue Flow   │
│  markstream-vue (SSE流式Markdown渲染)                    │
└───────────────────────┬──────────────────────────────────┘
                        │ HTTP/SSE (reasoning_content + content)
┌───────────────────────┴──────────────────────────────────┐
│                  Backend (FastAPI)                        │
│  LangGraph (Agent编排) │ Celery (异步) │ LangChain (RAG) │
└──┬────────┬────────┬────────┬────────┬──────────────────┘
   │        │        │        │        │
   ▼        ▼        ▼        ▼        ▼
┌──────┐ ┌─────┐ ┌──────┐ ┌──────┐ ┌──────┐
│PostgreSQL│ │Redis│ │MinIO │ │Ollama│ │Neo4j │
│+pgvector │ │     │ │      │ │      │ │(图谱)│
└──────┘ └─────┘ └──────┘ └──────┘ └──────┘
```

### 4.2 技术选型

| 层次 | 技术 | 用途 |
|------|------|------|
| 前端框架 | Vue 3 + Vite | SPA 应用 |
| UI 组件库 | Element Plus | 通用 UI（表单、卡片、布局） |
| AI 聊天组件 | **LiaoKit** (`@yuandezuohua/liaokit`) | AI 模式对话界面（SSE流式+Markdown+多窗口） |
| 流式渲染 | **markstream-vue** | SSE 流式 Markdown 打字机渲染（LiaoKit 底层引擎） |
| 富文本编辑 | Tiptap | 小说模式章节编辑 |
| 图表 | ECharts + vue-echarts | 节奏分析曲线/雷达图 |
| 流程图 | Vue Flow | V2 工作流可视化编辑 |
| 工具库 | @vueuse/core, dayjs | 组合式工具、日期格式化 |
| 后端框架 | FastAPI | REST API + SSE 流式 |
| AI 编排 | LangGraph + LangChain | Agent 框架 + RAG 管线 |
| DeepSeek 集成 | langchain-deepseek | DeepSeek Chat 模型集成 |
| 异步任务 | Celery（Redis broker） | AI 生成、章节分析 |
| 向量库 | pgvector（PostgreSQL 扩展） | 仿写知识库向量检索 |
| 图数据库 | **Neo4j** | 一致性知识图谱存储与查询 |
| 中文分词 | jieba | 实体提取辅助 |
| 嵌入模型 | BAAI/bge-small-zh-v1.5 | 中文向量化（本地 Mac/uzentu） |
| 对象存储 | MinIO | 封面、txt 上传 |
| 主力模型 | DeepSeek `deepseek-v4-pro` | 写作生成（context 1M, output 384K） |
| 轻量模型 | DeepSeek `deepseek-v4-flash` | 审读、分类、实体提取（便宜 10 倍） |
| 辅助模型 | Ollama 本地 | 极轻量任务 |
| 部署 | Docker Compose | uzentu（8GB/200GB） |

### 4.3 关键库清单

#### 后端（requirements.txt）

| 库 | 用途 |
|----|------|
| langgraph >= 1.0 | Agent 编排 + 工作流引擎 |
| langchain >= 0.3 | LLM 集成 + RAG 管线 |
| langchain-deepseek >= 0.1 | DeepSeek 模型适配 |
| celery >= 5.4 | 异步任务队列 |
| neo4j >= 5.28 | Neo4j Python 驱动 |
| networkx >= 3.4 | 内存图辅助分析（配合 Neo4j） |
| minio >= 7.2 | MinIO 客户端 |
| jieba >= 0.42 | 中文分词 |
| python-multipart >= 0.0.18 | 文件上传 |
| aiofiles >= 24.0 | 异步文件 IO |

#### 前端（package.json）

| 库 | 用途 |
|----|------|
| @yuandezuohua/liaokit | AI 聊天组件（SSE+Markdown+多窗口） |
| markstream-vue | 流式 Markdown 渲染引擎 |
| @tiptap/vue-3 + starter-kit + placeholder | 章节编辑器 |
| echarts + vue-echarts | 图表 |
| @vue-flow/core + background + controls | 工作流编辑器 |
| @vueuse/core | Vue 工具集 |
| dayjs | 日期处理 |

### 4.4 DeepSeek API 关键参数

```
模型选择:
  deepseek-v4-pro:  写作生成, context 1M, output 384K, $1.74/$3.48 per 1M tokens
  deepseek-v4-flash: 轻量任务, context 1M, output 384K, $0.14/$0.28 per 1M tokens

思考模式 (写作时必须开启):
  extra_body={"thinking": {"type": "enabled"}}
  reasoning_effort: "high" (v4-pro) / "minimal" (v4-flash 轻量任务)

流式输出:
  stream: true
  → SSE chunks 中包含 delta.reasoning_content 和 delta.content 两个字段
  → 前端分别渲染到思考面板和对话气泡

上下文缓存:
  prompt_cache_hit_tokens: 缓存命中的 token 数
  重复使用的系统提示和范例 → 自动缓存 → 降低成本
```

---

## 五、版本规划

### V1 — 可用的写作工具（当前阶段）

- [ ] 作品管理（主页 Dashboard + CRUD）
- [ ] 基本信息编辑
- [ ] 核心架构（种子 + 角色 + 世界观 → AI 生成大纲）
- [ ] 章节管理（左侧边栏）
- [ ] AI 写作（AI 模式对话 + 小说模式编辑，含思考过程展示）
- [ ] 知识库管理（上传 txt → 切片 → 向量入库）
- [ ] 仿写 RAG 引擎（场景类型匹配 + 检索 + 固定范例）
- [ ] 一致性知识图谱引擎（Neo4j 实体提取 + 检索设定）
- [ ] 写作智能体 + 读者智能体
- [ ] 节奏分析（简单图表实现）

**V1 不做：**
- [ ] 自定义智能体
- [ ] Skills 系统
- [ ] 工作流可视化编排（用固定工作流）
- [ ] 多用户/权限
- [ ] 移动端适配

### V2 — 可扩展的写作平台

- [ ] 工作流可视化编排（Vue Flow 拖拽）
- [ ] Skills 插件系统
- [ ] 用户自定义智能体
- [ ] 知识图谱可视化编辑（D3.js / Neo4j Browser 集成）
- [ ] 导出功能（txt）

### V3 — 智能化写作引擎

- [ ] 模型微调管线（DeepSeek API 微调 / 租 GPU 微调 Qwen）
- [ ] 自动伏笔管理（埋设→回收追踪）
- [ ] 章节间影响分析
- [ ] epub 导出

---

## 六、待讨论 & 已决议

| 事项 | 决议 |
|------|------|
| 版本管理 | V1 不做，后续按需加 |
| 章节生成方式 | 全部 AI 生成，前端展示思考过程（reasoning_content） |
| 导出格式 | V1 不做，V2 优先 txt |
| Skills vs Agent | Skills = 无身份的功能模块，Agent = 有角色的决策者。V2 做 Skills |
| 工作流编辑器 | V2 做，拖拽可视化 + 配置式双模式 |
| 仿写知识库 | 用 RAG 向量检索找相似片段，按场景类型分类，作为 AI 写作灵感参考 |
| 节奏分析 | 简单实现，低优先级 |
| 知识图谱存储 | Neo4j（Docker 部署在 uzentu），配合 NetworkX 做内存分析 |
| AI 聊天前端 | LiaoKit + markstream-vue，自建思考面板展示 reasoning_content |
