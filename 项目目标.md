# SynVerse（神韵）— AI 写作引擎

## 最终目标

一套私有 AI 写作引擎，学习文学审美、记住设定、适配你的创作流程。

---

## 两条并行路线

| 路线 | 目标 | 状态 |
|------|------|------|
| **主线：RAG 增强写作管线** | novel 流程 + RAG 检索，让 AI 写作时有小说原文参考 | 搭建中 |
| **副线：模型微调研究** | DeepSeek API 微调 / 租云 GPU 微调 Qwen2.5-8B | 研究阶段 |

主线先跑通，积累的训练数据可反哺副线。

---

## 主线：RAG 增强写作管线

不新建项目，在 novel 流程上**插入检索步骤**：

```
章节大纲 + 已写摘要
       │
       ▼
  ① ChromaDB 检索 ─── 搜"别人怎么写类似情节"
   bge-small-zh        返回 Top 5 参考段落
       │
       ▼
  ② 拼装 Prompt
   · 固定风格范例（精选，每次复用）
   · 小说总纲 + 角色设定
   · 已写章节摘要
   · 风格规则（novel/CLAUDE.md）
   · 检索到的参考段落
       │
       ▼
  ③ DeepSeek API 生成
       │
       ▼
  ④ 读者审读 → 修订
       │
       ▼
  ⑤ 保存 output/chapters/
```

### 技术选型

| 组件 | 选择 | 原因 |
|------|------|------|
| 向量库 | ChromaDB | 轻量，pip install，无需服务 |
| 嵌入模型 | bge-small-zh（110MB） | 中文检索，本地 Mac 跑 |
| 切片 | 按段落（双换行），200~500字/段 | 保持场景完整 |
| 检索 | 大纲文字 → 向量 → Top 5 | 语义相似搜索 |
| 生成 | DeepSeek API（deepseek-chat） | 主力写作 |

### 核心理念

- **检索找人怎么写类似情节**（情节参考），不是找文笔范例（范例固定复用）
- 小说原稿质量决定检索质量，入库前需检查编码和文本质量
- 管线跑通后再评估微调必要性

---

## 副线：模型微调

### 待验证

- 文笔模仿，微调 vs 范例，效果差距多大？
- 8B 模型微调后够不够用？
- DeepSeek API 微调 vs 租 GPU 自训，哪个划算？

### 路径

1. DeepSeek API 微调（推荐先试）：上传数据，按 token 计费
2. 租云 GPU：QLoRA 微调 Qwen2.5-8B，RTX 4090 约 5-10h，总花费约 10-20 元

---

## 已完成

- [x] 小说语料收集（6 本）
- [x] novel 项目验证（产出 4 章）
- [x] 风格对比实验（结论：范例 > 规则 > 裸模型）
- [x] 编码修复（GBK → UTF-8）
